"use client";

import { useEffect, useRef, useState } from "react";

declare global { interface Window { gtag?: (...args: unknown[]) => void; } }

type IntakeData = { situation: string; type: string; oppervlakte: string; adres: string; plaats: string; datum: string; urgentie: string; bereikbaarheid: string; vervuiling: string; toelichting: string; naam: string; telefoon: string; email: string; };
const EMPTY: IntakeData = { situation: "", type: "", oppervlakte: "", adres: "", plaats: "", datum: "", urgentie: "", bereikbaarheid: "", vervuiling: "", toelichting: "", naam: "", telefoon: "", email: "" };
const SITUATIONS = [["Woning leeghalen", "Ik wil een woning leeg en klaar laten opleveren."],["Na een overlijden", "Ik heb hulp nodig bij het leeghalen en opleveren van een woning."],["Huurwoning opleveren", "De woning moet klaar voor de sleuteloverdracht."],["Bedrijfspand opleveren", "Een bedrijfsruimte moet leeg of opleverklaar."],["Ik heb snel hulp nodig", "Er is weinig tijd voor de oplevering."],["Iets anders", "Mijn situatie staat hier niet tussen."]];
const STEPS = ["Uw situatie", "De ruimte", "Planning", "Contact"];
function trackEvent(name: string, parameters: Record<string, string> = {}) { window.gtag?.("event", name, parameters); }

export default function Contact() {
  const [step, setStep] = useState(0); const [data, setData] = useState<IntakeData>(EMPTY); const [submitted, setSubmitted] = useState(false); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState("");
  const submissionId = useRef<string>(crypto.randomUUID()); const intakeStarted = useRef(false);
  useEffect(() => { const stored = sessionStorage.getItem("oplevermeester_opleverplan"); if (!stored) return; try { const plan = JSON.parse(stored) as { deadline?: string; summary?: string }; setData((current) => ({ ...current, situation: "Inspectierapport / opleverpunten uitvoeren", datum: plan.deadline || current.datum, toelichting: plan.summary ? `Opleverpunten:\n${plan.summary}` : current.toelichting })); setStep(1); sessionStorage.removeItem("oplevermeester_opleverplan"); } catch { sessionStorage.removeItem("oplevermeester_opleverplan"); } }, []);
  function start() { if (!intakeStarted.current) { intakeStarted.current = true; trackEvent("intake_start", { source_page: window.location.pathname + window.location.search }); } }
  function setField(field: keyof IntakeData, value: string) { start(); setData((current) => ({ ...current, [field]: value })); }
  function chooseSituation(value: string) { start(); setData((current) => ({ ...current, situation: value })); trackEvent("intake_situation_selected", { source_page: window.location.pathname + window.location.search, situation: value }); setStep(1); }
  function next(nextStep: number) { start(); setStep(nextStep); trackEvent("intake_step", { source_page: window.location.pathname + window.location.search, step: String(nextStep + 1) }); }
  async function submit(e: React.FormEvent) { e.preventDefault(); if (submitting) return; setSubmitting(true); setError(""); const landingUrl = window.location.pathname + window.location.search + window.location.hash; const referrer = document.referrer || "Direct / onbekend"; const message = [`Hulpvraag: ${data.situation}`, `Object: ${data.type}`, `Locatie: ${data.adres}, ${data.plaats}`, `Oppervlakte: ${data.oppervlakte || "Onbekend"} m²`, `Opleverdatum: ${data.datum || "Nog niet bekend"}`, `Planning: ${data.urgentie}`, `Vervuiling: ${data.vervuiling || "Onbekend"}`, `Verdieping/bereikbaarheid: ${data.bereikbaarheid || "Onbekend"}`, `Toelichting: ${data.toelichting || "Niet toegelicht"}`, `Herkomst: ${referrer}`].join("\n"); try { const response = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: data.naam, email: data.email, phone: data.telefoon, message, sourcePage: landingUrl, submissionId: submissionId.current }) }); if (!response.ok) throw new Error("De intake kon niet worden verstuurd."); trackEvent("generate_lead", { source_page: landingUrl, object_type: data.type || "onbekend", planning: data.urgentie || "onbekend", situation: data.situation }); setSubmitted(true); } catch (cause) { setError(cause instanceof Error ? cause.message : "Versturen is niet gelukt."); } finally { setSubmitting(false); } }
  return <section id="intake" className="bg-white py-14 md:py-18"><div className="container-om mx-auto max-w-5xl"><div className="mb-8 max-w-2xl"><p className="text-sm font-semibold uppercase tracking-[0.12em] text-orange">Start intake</p><h2 className="mt-2 font-display text-3xl font-semibold tracking-tight text-navy md:text-4xl">Waar kunnen wij u mee helpen?</h2><p className="mt-3 leading-relaxed text-muted">Kies wat het beste bij uw situatie past. Wij helpen u vanaf daar verder.</p></div><div className="mb-5 flex gap-2" aria-label={`Stap ${step + 1} van 4`}>{STEPS.map((label, index) => <div key={label} className="min-w-0 flex-1"><div className={`h-1.5 rounded-full ${index <= step ? "bg-orange" : "bg-surface-mid"}`} /><span className={`mt-2 hidden text-xs sm:block ${index === step ? "font-semibold text-navy" : "text-muted"}`}>{label}</span></div>)}</div><div className="overflow-hidden rounded-om border border-surface-mid bg-surface-light p-5 md:p-8">{submitted ? <div className="py-10 md:py-14"><p className="text-sm font-semibold text-orange">Wij hebben uw situatie ontvangen</p><h3 className="mt-2 font-display text-2xl font-semibold text-navy md:text-3xl">Duidelijk. Wij pakken hem vanaf hier op.</h3><p className="mt-3 max-w-xl text-muted">We bekijken wat u heeft doorgegeven en nemen contact met u op om samen de volgende stap te bepalen.</p></div> : <form onSubmit={submit} onFocusCapture={start}>{step === 0 && <Slide eyebrow="Uw situatie" title="Waar kunnen wij u mee helpen?" text="U hoeft het nog niet precies te weten. Kies wat het dichtst bij uw situatie komt."><div className="grid gap-3 sm:grid-cols-2">{SITUATIONS.map(([label, description]) => <button key={label} type="button" onClick={() => chooseSituation(label)} className="group rounded-om border border-surface-mid bg-white p-5 text-left transition hover:-translate-y-0.5 hover:border-orange hover:shadow-sm focus:border-orange focus:outline-none"><span className="flex items-start justify-between gap-4"><span><strong className="block text-base text-navy">{label}</strong><span className="mt-1.5 block text-sm leading-relaxed text-muted">{description}</span></span><span aria-hidden="true" className="text-xl text-orange">→</span></span></button>)}</div></Slide>}{step === 1 && <Slide eyebrow={data.situation.startsWith("Inspectierapport") ? "Uw uitvoeringsplan staat klaar" : "Duidelijk"} title="Dan kijken we nu even naar de ruimte." text="Een paar basisgegevens zijn genoeg. Wat u niet precies weet, kunt u leeg laten."><div className="grid gap-4 md:grid-cols-2"><Select label="Wat voor ruimte is het?" value={data.type} onChange={(v) => setField("type", v)} options={["Woning", "Appartement", "Bedrijfspand", "Kantoor", "Opslagruimte", "Winkelruimte", "Anders"]} required /><Input label="Plaats" value={data.plaats} onChange={(v) => setField("plaats", v)} required /><Input label="Adres" value={data.adres} onChange={(v) => setField("adres", v)} required /><Input label="Ongeveer hoeveel m²? (optioneel)" type="number" value={data.oppervlakte} onChange={(v) => setField("oppervlakte", v)} /></div><Nav back={() => setStep(0)} next={() => next(2)} disabled={!data.type || !data.plaats || !data.adres} /></Slide>}{step === 2 && <Slide eyebrow="Helder" title="Wanneer moet het geregeld zijn?" text="Hiermee kunnen we beter inschatten wat er nodig is en hoe snel we moeten schakelen."><div className="grid gap-4 md:grid-cols-2"><Select label="Hoe snel is hulp nodig?" value={data.urgentie} onChange={(v) => setField("urgentie", v)} options={["Er is ruimte in de planning", "Binnen 2 weken", "Binnen 1 week", "Spoed"]} required /><Input label="Uiterste opleverdatum (optioneel)" type="date" value={data.datum} onChange={(v) => setField("datum", v)} /><Select label="Bereikbaarheid (optioneel)" value={data.bereikbaarheid} onChange={(v) => setField("bereikbaarheid", v)} options={["Begane grond", "Verdieping met lift", "1 verdieping zonder lift", "Meerdere verdiepingen zonder lift", "Anders / onbekend"]} /><Select label="Hoe staat de ruimte erbij? (optioneel)" value={data.vervuiling} onChange={(v) => setField("vervuiling", v)} options={["Licht", "Normaal", "Zwaar", "Zeer zwaar", "Weet ik niet"]} /></div><label className="mt-4 block text-sm font-medium text-navy">Wilt u nog iets meegeven? <span className="font-normal text-muted">(optioneel)</span><textarea rows={5} value={data.toelichting} onChange={(e) => setField("toelichting", e.target.value)} className="mt-1.5 w-full rounded-om border border-surface-mid bg-white px-4 py-3 text-navy outline-none focus:border-orange" /></label><Nav back={() => setStep(1)} next={() => next(3)} disabled={!data.urgentie} /></Slide>}{step === 3 && <Slide eyebrow="We hebben een goed beeld" title="Waar kunnen we u bereiken?" text="Laat uw gegevens achter. Wij bekijken de situatie en nemen persoonlijk contact met u op."><div className="grid gap-4 md:grid-cols-2"><Input label="Naam" value={data.naam} onChange={(v) => setField("naam", v)} required /><Input label="Telefoon" type="tel" value={data.telefoon} onChange={(v) => setField("telefoon", v)} required /><div className="md:col-span-2"><Input label="E-mail" type="email" value={data.email} onChange={(v) => setField("email", v)} required /></div></div>{error && <p role="alert" className="mt-4 text-sm text-red-700">{error} Probeer het opnieuw.</p>}<div className="mt-6 flex items-center justify-between gap-3"><button type="button" onClick={() => setStep(2)} className="px-2 py-3 text-sm font-semibold text-muted">← Terug</button><button type="submit" disabled={submitting || !data.naam || !data.telefoon || !data.email} className="rounded-om bg-orange px-6 py-3.5 font-semibold text-white disabled:opacity-50">{submitting ? "Versturen…" : "Laat ons helpen →"}</button></div></Slide>}</form>}</div></div></section>;
}
function Slide({ eyebrow, title, text, children }: { eyebrow: string; title: string; text: string; children: React.ReactNode }) { return <div><p className="text-sm font-semibold text-orange">{eyebrow}</p><h3 className="mt-1 font-display text-2xl font-semibold text-navy">{title}</h3><p className="mb-6 mt-2 max-w-2xl text-sm leading-relaxed text-muted">{text}</p>{children}</div>; }
function Nav({ back, next, disabled }: { back: () => void; next: () => void; disabled?: boolean }) { return <div className="mt-6 flex items-center justify-between gap-3"><button type="button" onClick={back} className="px-2 py-3 text-sm font-semibold text-muted">← Terug</button><button type="button" onClick={next} disabled={disabled} className="rounded-om bg-orange px-6 py-3.5 font-semibold text-white disabled:opacity-50">Verder →</button></div>; }
function Input({ label, value, onChange, type = "text", required }: { label: string; value: string; onChange: (value: string) => void; type?: string; required?: boolean }) { return <label className="block text-sm font-medium text-navy">{label}<input type={type} value={value} onChange={(e) => onChange(e.target.value)} required={required} className="mt-1.5 w-full rounded-om border border-surface-mid bg-white px-4 py-3 text-navy outline-none focus:border-orange" /></label>; }
function Select({ label, value, onChange, options, required }: { label: string; value: string; onChange: (value: string) => void; options: string[]; required?: boolean }) { return <label className="block text-sm font-medium text-navy">{label}<select value={value} onChange={(e) => onChange(e.target.value)} required={required} className="mt-1.5 w-full rounded-om border border-surface-mid bg-white px-4 py-3 text-navy outline-none focus:border-orange"><option value="">Kies wat past</option>{options.map((option) => <option key={option}>{option}</option>)}</select></label>; }
